perf(mobile-app): R5 structured ApiError end-to-end plus AUTH auto-redirect

Continuing the Callstack react-native-best-practices pass (round 5).

Structured errors propagated to UI:
- store/index.ts: RequestGuard stores full ApiError instance instead of just message
- PageContainer: error prop typed as ApiError; CTA differentiates AUTH vs other errors

Actionable error recovery:
- All 4 data screens: onRetry navigates to SettingsTab on AUTH error, retries fetch otherwise
- Logs/Models/Providers: navigation prop typed via BottomTabNavigationProp

Cleanup:
- DashboardScreen: extracted inline 10000 literal to POLL_INTERVAL_MS constant

Wiki: updated mobile-app-performance.md (sections 29-32) and log.md.
Validation: npx tsc --noEmit passes clean.
